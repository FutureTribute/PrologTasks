% Модифікувати предикат, що обчислює суму елементів списку
% так, щоб він обчислював множення елементів списку

prod([], 1).
prod([H|T], R) :- prod(T, R_), R is R_ * H.

% prod([], X). -> X = 1
% prod([2, 2, 4], X). -> X = 16